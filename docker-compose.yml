services:
  trading-research:
    build: .
    container_name: trading-strategy-research
    command: python main.py
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true
